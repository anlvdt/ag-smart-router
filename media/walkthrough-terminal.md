# Terminal Command Management

Grav evaluates every terminal command against a whitelist/blacklist system.

**Built-in:** 130+ safe commands (npm, git, docker, python, cargo, kubectl...)

**Customize:**
- Add/remove commands from your personal whitelist
- Block dangerous commands with substring or `/regex/` patterns
- Test any command to see if it would be allowed or blocked
